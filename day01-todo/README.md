# Day 01 — Todo

A single-file todo app: add, complete, delete, filter (all/active/done), clear completed. Persists to `localStorage`, no build step.

Open `index.html` directly in a browser to run it.
